---
import Layout from "../layouts/Layout.astro";
import "../styles/genericPageContent.css";

const pages = import.meta.glob("../../content/pages/*.md", { eager: true });
const contact = pages["../../content/pages/contact.md"];
const { frontmatter, Content } = contact;
---

<Layout title="Contact — Raphaelle Brui" bodyClass="page contact">
  <main class="page-content">
    <h1>{frontmatter.title}</h1>

    <Content />
  </main>
</Layout>
